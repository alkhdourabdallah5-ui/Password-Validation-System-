# Password-Validation-System-
A simple Java command-line program that checks whether a password meets basic security requirements. It validates length and confirms the presence of uppercase letters, lowercase letters, and digits, then prints whether the password is valid or invalid.
